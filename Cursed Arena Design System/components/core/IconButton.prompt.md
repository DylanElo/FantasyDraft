Square icon tap target for header bars and HUD chrome.

```jsx
<IconButton onClick={openSettings}><Settings size={20} /></IconButton>
```

Set `active` to highlight (e.g. the currently open tab in a segmented row of icon buttons).
