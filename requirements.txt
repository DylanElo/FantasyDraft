# No runtime dependencies — the app runs entirely in the browser.
# Python is only used for optional local dev server (python3 main.py)
# and for regenerating docs/characters_data.js from jjk_bot/characters.py.
