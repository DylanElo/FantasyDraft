## What changed

- introduced typed duration clocks and status families across all 78 First Creation skill contracts
- added frozen effect context for caster, recipient, original, primary, secondary, and resolved targets
- fixed character-slot watchers and separated Six Eyes watcher consumption from its future damage buff
- made RCT cleanse precise to affliction/soul families
- revoked Geto replacement skills when Curse Stock falls below threshold
- made Kamo's Blood Mark drain seeded, random, correctly timed, and one-shot
- added explicit Todo alternate redirect and Venom Bloom primary/secondary payloads through server and Phaser
- restricted Miwa's counter to melee and fixed Divergent Fist trigger/consume behavior
- aligned adjusted costs, stuns, replacement slots, target ordering, redirect targets, and disabled reasons between server and Phaser

## Why

The prior explicit-skill pass removed text inference, but timing still depended on global cleanup and several multi-step effects read mutable or team-wide state. This caused setup windows, watchers, cleansing, replacements, drains, redirects, and multi-target skills to diverge from their documented contracts.

## Validation

- `python -m pytest -q` — 221 passed, 1 opt-in browser visual test skipped
- `python -m compileall -q jjk_arena web/app.py`
- `node --check` on the three modified Phaser modules
- `git diff --check`

## Scope

No characters, screens, visual effects, progression, layout changes, or UI redesign are included. The separate mobile UI replacement branch remains intentionally out of scope.
